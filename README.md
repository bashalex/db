# DataBases HSE Course.

In order to generate .data file, writeTestBinaryFile() function from the Main file must be called.

.meta file is supposed to contain the following lines:

```
id;int
name;varchar;10
dt;datetime
```
